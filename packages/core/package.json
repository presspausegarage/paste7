{
  "name": "@paste7/core",
  "version": "0.0.0",
  "private": true,
  "description": "PHI rule-pack engine: format walkers, per-format rule packs, redaction primitives. Pure TypeScript, no UI, no Tauri dependencies.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "fast-check": "^3.23.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
